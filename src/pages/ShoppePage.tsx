import UITabContent from "~/components/UITabContent";
import { tabContent } from "~/config/tabShoppe.config";

const ShoppePage = () => {
  return <UITabContent tabData={tabContent} />;
};
export default ShoppePage;
